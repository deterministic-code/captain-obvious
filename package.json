{
  "name": "@deterministic-code/captain-obvious",
  "version": "0.1.2",
  "description": "Deterministic, mechanical lint gates + Claude Code guard hooks, installable into any repo.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/deterministic-code/captain-obvious.git"
  },
  "bin": {
    "captain-obvious": "dist/bin/captain-obvious.js",
    "captain-obvious-install": "bin/install.mjs",
    "captain-obvious-lint": "bin/lint.mjs"
  },
  "exports": {
    "./install": "./bin/install.mjs",
    "./package.json": "./package.json"
  },
  "files": [
    "bin",
    "lib",
    "hooks",
    "dist",
    "db",
    "web/dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "test": "vitest run --coverage"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.1",
    "jscpd": "^3.5.10",
    "knip": "^6.24.0",
    "prettier": "^3.4.0",
    "typescript": "^5.7.0",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^2.1.9",
    "vitest": "^2.1.0"
  }
}
