node_modules/
*.log
.DS_Store
dist/
*.tsbuildinfo
data/*.db

# The prebuilt control-panel bundle is shipped (see package.json "files") and
# has no source in this repo, so it must be committed despite the dist/ rule.
!web/dist/
